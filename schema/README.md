# schema/

Generated output lands here, one folder per database:

```
schema/
  MMC_V10/
    MANIFEST.md            written by Publish-SchemaExport.ps1
    00-inventory.txt
    01-foundation.sql
    ...
```

Nothing in here is hand-edited. It is regenerated by
`Export-SytelineSchema.ps1` and copied in by `scripts/Publish-SchemaExport.ps1`.
See `PUBLISHING.md`.

**Schema only — no table data.** See `PUBLISHING.md` for what may and may not
be committed.
